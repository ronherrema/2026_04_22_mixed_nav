This repository is meant to provide a template for Expo/React Native using nexted navigators - specifically, a Stack navigator nested in a Tab navigator. Once you have cloned the repo, simply go into the project folder in your CLI and enter 'npm install', after which 'npx expo start' should successfully launch the app.
